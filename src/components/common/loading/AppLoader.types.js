import PropTypes from 'prop-types';

export const AppLoaderPropTypes = {
  size: PropTypes.number,

  text: PropTypes.string,

  fullScreen: PropTypes.bool,

  overlay: PropTypes.bool,

  sx: PropTypes.object,
};

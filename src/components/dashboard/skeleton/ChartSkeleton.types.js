import PropTypes from 'prop-types';

export const ChartSkeletonPropTypes = {
  height: PropTypes.oneOfType([PropTypes.number, PropTypes.string]),
};

export const ChartSkeletonDefaultProps = {
  height: 320,
};

import PropTypes from 'prop-types';

export const DashboardRowPropTypes = {
  children: PropTypes.node,

  spacing: PropTypes.number,
};

export const DashboardRowDefaultProps = {
  spacing: null,
};

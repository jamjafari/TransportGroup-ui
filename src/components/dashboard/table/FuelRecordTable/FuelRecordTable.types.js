import PropTypes from 'prop-types';

export const FuelRecordTablePropTypes = {
  height: PropTypes.oneOfType([PropTypes.number, PropTypes.string]),
};

export const FuelRecordTableDefaultProps = {
  height: 450,
};

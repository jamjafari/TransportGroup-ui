import PropTypes from 'prop-types';

export const LatestActivitiesTablePropTypes = {
  height: PropTypes.oneOfType([PropTypes.number, PropTypes.string]),
};

export const LatestActivitiesTableDefaultProps = {
  height: 420,
};

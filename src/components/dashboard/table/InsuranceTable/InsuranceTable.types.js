import PropTypes from 'prop-types';

export const InsuranceTablePropTypes = {
  height: PropTypes.oneOfType([PropTypes.number, PropTypes.string]),
};

export const InsuranceTableDefaultProps = {
  height: 450,
};
